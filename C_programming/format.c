#include <stdio.h>

/**
 * main - print the format
 *
 *Return: 0 as success
 */

int main(void)
{
	char s = 'G';
	int z = 340 + 60;
	float l = 40.0 + 0.40;

	printf("value of s = %C\n", s);
	printf("value of z = %i\n", z);
	printf("value of l = %f\n", l);
	return (0);
}
